# === Core ML ===
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.4.2
scipy==1.13.0

# === Deep Learning (CPU build for local dev; HPC uses CUDA build) ===
torch==2.3.0
torchvision==0.18.0

# === Recommender Systems ===
surprise==0.1          # collaborative filtering baseline
implicit==0.7.2        # ALS / BPR

# === LLM / LangChain (Phase 3) ===
# Let pip resolve compatible versions across the langchain ecosystem
langchain==0.2.6
langchain-community==0.2.6
langchain-core>=0.2.19,<0.3.0
langchain-groq==0.1.9  # cloud inference (HF Spaces)
ollama==0.2.1          # local inference
groq==0.9.0

# === Embeddings (HF Spaces uses sentence-transformers, local uses Ollama) ===
sentence-transformers==3.0.1

# === Vector DB (Phase 5) ===
faiss-cpu==1.8.0

# === Agent / Graph (Phase 4) ===
langgraph==0.1.9

# === Backend + UI (Phase 6) ===
fastapi==0.111.0
uvicorn[standard]==0.30.1
gradio==4.37.1
huggingface_hub==0.23.4
pydantic==2.7.4

# === Data / EDA ===
matplotlib==3.9.0
seaborn==0.13.2
plotly==5.22.0

# === Notebooks ===
jupyterlab==4.2.2
ipywidgets==8.1.3

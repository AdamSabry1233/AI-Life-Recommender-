# Install on top of requirements.txt:  pip install -r requirements-dev.txt
-r requirements.txt

# Test runner + coverage
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0

# Filesystem-watch auto-rerun (modern replacement for the unmaintained pytest-watch)
pytest-watcher>=0.4.3

# Linting (optional but used by CI)
ruff>=0.6.0
